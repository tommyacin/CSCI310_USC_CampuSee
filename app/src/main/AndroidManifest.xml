<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campusee">

    <application
        android:name=".Global"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".publisher_page_of_events"></activity>
        <activity android:name=".activity_map" />
        <activity android:name=".NotificationPage" />
        <activity android:name=".StudentHome" />
        <activity android:name=".IconPage" />
        <activity android:name=".PublishEvent" />
        <activity android:name=".EditEvent" />
        <activity android:name=".CreateEvent" />
        <activity android:name=".PublisherMain" />
        <activity android:name=".PublisherSignup" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".GeofenceTransitionService" />
        <service android:name=".EventCreatedNotificationService" />



        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="ADD API KEY HERE"/>
    </application>


    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- Required if your app targets Android 10 (API level 29) or higher -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>


</manifest>